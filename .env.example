# Informações da aplicação
APP_NAME=Laravel
APP_ENV=production
APP_KEY=base64:qsOFbyII6y7ArFbZOU11tR7/WEE9fqfkqch1LdLnzJU
APP_DEBUG=false
APP_URL=https://SEU-PROJETO.up.railway.app  # 🔁 Substitua pelo URL do Railway

# Logs
LOG_CHANNEL=stack
LOG_LEVEL=debug

# Banco de Dados (Railway)
DB_CONNECTION=mysql
DB_HOST=turntable.proxy.rlwy.net
DB_PORT=22556
DB_DATABASE=contatos_db
DB_USERNAME=root
DB_PASSWORD=YIjHSILNYwLCktgMlxxIKPdqvwKtkoK

# Sessões e cache (mantidos locais)
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Fila de jobs (execução síncrona — não usa Redis)
QUEUE_CONNECTION=sync

# Sistema de arquivos
FILESYSTEM_DISK=local

# E-mail (desabilitado por padrão — configure se for usar)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME="${APP_NAME}"

# Vite (somente necessário se usar front-end com Vite)
VITE_APP_NAME="${APP_NAME}"
